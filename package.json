{
  "name": "todo-app",
  "version": "0.1.0",
  "description": "A simple todo list app for testing alpha-loop",
  "type": "module",
  "scripts": {
    "dev": "vite --port 4173",
    "build": "vite build",
    "preview": "vite preview --port 4173",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "vitest": "^2.1.0",
    "vite": "^6.0.0"
  }
}
